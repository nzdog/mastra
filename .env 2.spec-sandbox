# spec-sandbox Environment Configuration
# This is a template for the spec-sandbox deployment environment
# Actual values should be set in Railway/GitHub Secrets

# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Environment identifier
NODE_ENV=staging
ENVIRONMENT_NAME=spec-sandbox
BRANCH_NAME=feature/memory-layer-spec

# Optional - CORS Configuration
# Allow requests from spec-sandbox specific domains
# ALLOWED_ORIGINS=https://spec-sandbox.yourdomain.com

# Optional - Redis for session storage
# REDIS_URL=redis://:password@hostname:6379

# Spec-specific feature flags
ENABLE_AUDIT_LOGGING=true
ENABLE_MEMORY_LAYER_SPEC=true
ENABLE_POLICY_GATES=true

# Rate limiting (stricter for sandbox)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50
