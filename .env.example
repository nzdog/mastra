# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional - CORS Configuration (Phase 1.2: Hardened CORS)
# Comma-separated list of allowed origins for API requests
# If not provided, defaults to localhost origins for development
# Production: MUST be set (no wildcard allowed)
# Examples:
#   Development: http://localhost:3000,http://localhost:5173
#   Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Allow credentials (cookies, authorization headers) in CORS requests
# Default: false (safer)
# CRITICAL: Cannot be true with wildcard (*) origin
CORS_ALLOW_CREDENTIALS=false

# Preflight cache duration in seconds
# How long browsers cache preflight (OPTIONS) responses
# Default: 600 (10 minutes)
CORS_MAX_AGE=600

# Allowed HTTP methods for CORS requests
# Default: GET,POST,PUT,PATCH,DELETE,OPTIONS
CORS_ALLOW_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS

# Allowed request headers
# Default: Content-Type,Authorization,X-Requested-With,X-API-Version,X-Trace-ID
CORS_ALLOW_HEADERS=Content-Type,Authorization,X-Requested-With,X-API-Version,X-Trace-ID

# Headers exposed to client (can be read by JavaScript)
# Default: X-API-Version,X-Spec-Version
CORS_EXPOSE_HEADERS=X-API-Version,X-Spec-Version

# Optional - Redis for persistent session storage (production)
# If not provided, uses in-memory storage (ephemeral)
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://:password@hostname:6379  # For Railway, Heroku, etc.

# Optional - Email Notifications
# Enable/disable email notifications for new diagnostic walk sessions
# Default: false (disabled)
EMAIL_NOTIFICATIONS_ENABLED=false

# Email recipient for notifications
# Default: coherence@lichenprotocol.com
EMAIL_NOTIFICATION_RECIPIENT=coherence@lichenprotocol.com

# SMTP Configuration (required if EMAIL_NOTIFICATIONS_ENABLED=true)
# Host: Your SMTP server hostname
# Port: SMTP port (587 for TLS, 465 for SSL)
# Secure: Set to 'true' for port 465, 'false' for port 587
# User: SMTP username (usually your email address)
# Pass: SMTP password or app-specific password
#
# Example for Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_SECURE=false
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-specific-password
#
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@example.com
# SMTP_PASS=your_smtp_password_here
